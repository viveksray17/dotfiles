#!/bin/sh
connection_name=$(nmcli connection show | grep -w -v "[--]" | tail -n +2 | awk -F " {2,}" '{ print $3": " $1" ("$4")" }')
[ "$connection_name" ] && echo "$connection_name" || echo "disconnected"
