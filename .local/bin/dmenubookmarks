#!/bin/sh
chosen=$(cat $HOME/.local/share/bookmarks/bookmarks.txt | awk '{ print $1 }' | sort | dmenu -i -p "choose bookmarks:")
[ -z "$chosen" ] && exit
url=$(cat $HOME/.local/share/bookmarks/bookmarks.txt | grep -w "$chosen" | awk '{ print $2 }')
firefox $url
